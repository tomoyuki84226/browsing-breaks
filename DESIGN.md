# Browsing Breaks Chrome拡張 設計書

キャッチコピー: **Take breaks while browsing.**

## 1. 概要

Webサイトの閲覧を一定間隔で中断し、簡単な足し算への回答を促すChrome拡張機能を作成する。

指定したWebサイトで条件を満たすURL遷移が発生すると、ページ全体を覆うモーダルを表示する。利用者が正解するまでモーダルを閉じることはできず、背後のページ内容を閲覧・操作できない。

## 2. 目的

- Web閲覧中に定期的な小休止・注意喚起のタイミングを作る。
- URLごとに、時間またはページ移動回数を基準として出題頻度を設定できるようにする。
- 一般的なWebサイトだけでなく、URLだけが変化するSPA（Single Page Application）でも動作させる。

## 3. 対象範囲

### 3.1 対象

- Chrome拡張機能 Manifest V3
- 通常の `http://` および `https://` ページ
- ページ再読み込み、通常のページ遷移、履歴移動、およびSPA内のURL変更
- 複数のサイト設定の登録、編集、有効化、無効化、削除

### 3.2 対象外・制約

Chromeの仕様上、次のページにはコンテンツスクリプトを注入できないため動作対象外とする。

- `chrome://` で始まるChrome内部ページ
- Chromeウェブストア
- 他の拡張機能のページ
- ブラウザの新しいタブなど、Chromeが注入を禁止しているページ
- PDFビューアーなど、コンテンツスクリプトが正常に動作しない特殊ページ

「すべてのWebサイトで動作する」とは、拡張機能が通常の `http://` / `https://` ページで動作可能であり、その中から設定したURLに一致するページを制御対象にできることを意味する。

## 4. 用語

| 用語 | 意味 |
| --- | --- |
| サイト設定 | サイト名、URL正規表現、間隔方式、間隔値をまとめた設定 |
| 対象URL | 有効なサイト設定のURL正規表現に一致するURL |
| URL遷移 | タブ内で表示URLが別のURLへ変わること。SPA内の変更も含む |
| 論理URL遷移 | 1回のURL変更操作を、複数の検知手段から通知されても1回にまとめたもの |
| 時間方式 | 指定分数が経過した後の、次の対象URL遷移で出題する方式 |
| 移動回数方式 | 対象URLへの遷移が指定回数に達するたびに出題する方式 |
| ロックモーダル | 正解するまでページ全体を覆い、閲覧と操作を禁止する画面 |

## 5. 機能要件

### 5.1 サイト設定

設定画面で、サイト設定を複数登録できる。

| 項目 | 型 | 内容 |
| --- | --- | --- |
| サイト名（任意） | 文字列 | 利用者が設定を識別するための表示名。未入力でも保存できる |
| URL正規表現 | 文字列 | 対象URLを判定するJavaScript互換の正規表現 |
| 間隔方式 | 選択値 | `時間（分）` または `移動回数` |
| 間隔値 | 整数 | `1`以上。方式に応じて分数または回数として扱う |
| 有効状態 | 真偽値 | 利用者が新規追加する際の初期値は有効。設定を削除せず一時停止できる |

#### 初期サイト設定

`chrome.storage.sync` に `siteRules` キーが存在しない場合に限り、あらかじめ定義されたサイト設定を作成する。すでにキーが存在する場合は、値が空配列であっても初期設定を追加・復元しない。これにより、同期済みの設定および利用者が全設定を削除した状態を維持する。

初期サイト設定を追加・変更する場合は、`src/shared/constants.js` の `createInitialSiteRules` に定義する。サイト名、URL正規表現、間隔方式、間隔値、有効状態といった個別の設定内容は設計書へ重複記載せず、この関数を正とする。各設定には生成時に一意なIDと同一の作成・更新日時を付与する。

#### 入力チェック

- サイト名は任意とし、空文字を許可する。
- URL正規表現は保存前に `new RegExp(入力値)` で構文検証する。
- 間隔値はJavaScriptで安全に扱える1以上の整数のみ許可する。
- 入力エラーがある間は保存できず、該当項目の近くに理由を表示する。
- 正規表現はURL全体に対して評価する。例: `^https://(www\.)?example\.com/`

#### 設定一覧

- 登録済み設定を一覧表示する。
- サイト名が未入力の場合は、URL正規表現を設定名の代わりに表示する。
- 各設定を編集、削除、有効化、無効化できる。
- URL正規表現が同じ設定の重複登録は警告する。ただし、用途を分けられるよう保存自体は許可する。

### 5.2 共通表示設定

ロックモーダルの配色を設定画面から変更できる。配色はサイト設定ごとではなく、すべてのサイトで共通とする。

| 項目 | 型 | 内容 | 初期値 |
| --- | --- | --- | --- |
| 文字色 | カラーコード | モーダル内の文字に使用する基準色 | `#c9d1d9` |
| 背景色 | カラーコード | ページ全体を覆うモーダルの背景色 | `#000000` |

- 色は `#RRGGBB` 形式で保存する。
- 設定画面ではカラーピッカーとカラーコード入力欄を同期して表示する。
- 不正なカラーコードは保存できず、入力欄の近くにエラーを表示する。
- 設定変更後に新しく表示するモーダルから反映する。
- 出題中に設定が変更された場合は、表示中のモーダルにも即時反映する。
- 入力欄、ボタン、エラーメッセージなどの補助色は、設定された文字色と背景色を基準に視認性を保つよう生成する。

### 5.3 URL遷移の検知

次のイベントをURL遷移として検知する。

- リンククリックなどによる通常のページ遷移
- アドレスバーへのURL入力
- リロード
- 戻る・進む操作
- SPAの `history.pushState`、`history.replaceState`、`popstate` によるURL変更
- 同一ページ内のハッシュ変更

遷移先URLが有効なサイト設定に一致した場合のみ、その設定の判定処理を行う。

初回インストール直後にすでに開かれているページ、または新規タブで最初に開いたURLは基準点とし、原則として移動回数へ加算しない。リロードはURLが同一でも明示的な閲覧操作として1回に加算する。

同じブラウザ操作を複数の検知手段が通知した場合は、1回の論理URL遷移として扱う。実装では次の規則を用いる。

- 通常遷移とリロードは `webNavigation.onBeforeNavigate` を基準に検知する。
- `history.pushState` と `history.replaceState` は `webNavigation.onHistoryStateUpdated`、ハッシュ変更は `webNavigation.onReferenceFragmentUpdated` を基準に検知する。
- Content ScriptによるURL監視は、`webNavigation` の検知漏れを補うために使用する。
- SPAについて、同一タブで同じ到達URLが `webNavigation` とContent Scriptの両方から通知された場合は、通知順や通知間隔にかかわらず1回だけ加算する。
- 同じURLでも通常のページ再読み込みは別の閲覧操作として1回加算する。
- SPAで表示URL自体が変化しない `replaceState` などは、URL遷移として加算しない。

この重複排除はサイト名やDOM構造に依存させず、YouTube Shortsを含む一般的なSPAへ共通に適用する。

### 5.4 出題タイミング

#### 時間方式

1. サイト設定ごとに、最後に正解した日時を保持する。
2. まだ正解履歴がない場合は、設定を保存した日時を基準日時とする。
3. 基準日時から設定値の分数が経過しているか確認する。
4. 経過後、対象URLへの次の遷移時にロックモーダルを表示する。
5. 正解時に基準日時を更新し、次の時間計測を開始する。

時間経過だけでは閲覧中のページを突然ロックせず、次の対象URL遷移を契機とする。

#### 移動回数方式

1. サイト設定ごとに対象URLへの移動回数を保持する。
2. 対象URLへの遷移ごとに回数を1増やす。
3. 回数が設定値に達した遷移でロックモーダルを表示する。
4. 正解時に回数を0へ戻す。

#### 12時間経過時の初期化

長時間利用していなかった後の最初の対象URL遷移では休憩を挟まない。出題条件の判定より前に、サイト設定ごとに次の初期化判定を行う。

- 時間方式では、最後の基準日時 `lastSolvedAt` から12時間以上経過していた場合、現在の遷移では出題せず、`lastSolvedAt` を現在日時へ更新する。
- 移動回数方式では、最後に対象URLへ移動した日時 `lastNavigationAt` から12時間以上経過していた場合、現在の遷移では出題せず、移動回数を0へ戻して `lastNavigationAt` を現在日時へ更新する。
- 12時間ちょうども初期化対象とする。
- 初期化を発生させた現在の遷移は、移動回数へ加算しない。
- 次の対象URL遷移から通常の時間判定または移動回数の加算を再開する。
- 判定はブラウザ起動時、ページ復元時、および対象URL遷移時に行う。ブラウザ終了イベントだけには依存しない。
- 出題状態のまま12時間経過し、その後ページが復元または遷移された場合は、古い出題状態を削除する。拡張機能がタブをミュートしていた場合は、出題前の状態へ戻す。
- ページを開いたまま12時間に到達しただけでは表示中のモーダルを突然解除せず、ページ復元または次のURL遷移を契機に判定する。

設定データの `updatedAt` は設定内容の編集日時であり、この12時間判定には使用しない。間隔方式またはURL正規表現の変更で状態をリセットする場合は、変更時刻を新しい基準日時として使用する。

#### 複数設定が一致する場合

- 1つのURLに複数の有効設定が一致した場合、条件を満たした設定のうち1件だけを出題する。
- 優先順位は設定一覧の上から順とする。
- 1問に正解しても、同じ遷移で別設定のモーダルを連続表示しない。
- 出題されなかった設定の期限・移動回数は維持し、次の対象URL遷移で再判定する。

### 5.5 足し算問題

- 2つの整数をランダムに生成し、`数字A + 数字B = ?` の形式で表示する。
- 初期仕様では各整数を1～9とし、答えは2～18とする。
- 同じ問題を表示中に再生成しない。
- 回答は整数のみ受け付ける。
- Enterキーまたは回答ボタンで採点する。
- 不正解の場合はエラーメッセージを表示し、同じ問題への再回答を求める。
- 正解の場合は状態を更新し、モーダルを解除する。

問題の難易度や出題範囲の設定は初期版の対象外とする。

### 5.6 ロックモーダル

- ビューポート全体を覆う不透明なオーバーレイを表示する。
- 背景のページ内容を読めない程度に隠す。
- 背景ページへのマウス、タッチ、キーボード操作、スクロールを遮断する。
- 閉じるボタン、キャンセルボタン、背景クリックによる解除は設けない。
- モーダル内には問題、回答入力欄、回答ボタン、エラー表示のみを配置する。
- 表示直後に回答入力欄へフォーカスする。
- `Esc` キーでは閉じない。
- ページ側のCSSやJavaScriptの影響を受けにくくするため、Shadow DOM内にUIを構築し、最大級の `z-index` を使用する。
- モーダル要素がページ側スクリプトによって削除された場合は、出題中である限り再描画する。
- 共通表示設定の文字色と背景色を適用する。
- 出題中に同じタブで別URLへ遷移した場合、移動先が出題元の有効なサイト設定に引き続き一致する間は、同じ問題とロックモーダルを維持する。
- 移動先が出題元のサイト設定に一致しない場合は、ロックモーダルを解除し、拡張機能が変更したミュート状態を元へ戻す。出題元ルールの基準日時や移動回数はリセットしないため、対象URLへ戻った際は通常の判定によって再び出題される。
- Service Workerが出題中状態を削除する場合は、同じタブのContent Scriptへ解除通知を送り、保存状態と表示中のロックモーダルを同期して解除する。通知先が通常遷移によってすでに破棄されている場合の送信失敗は無視する。
- 移動先が別の有効なサイト設定に一致する場合は、出題元のロックを解除した後、その設定を同じ遷移で通常どおり判定する。別設定でも出題が必要な場合は、元のミュート状態を新しい出題へ引き継ぐ。

注: 拡張機能自体を無効化・削除する操作や、Chromeの開発者ツールを使った意図的な回避を完全に防止することはできない。

### 5.7 動画・音声を自動再生するサイトへの対応

YouTubeなど、ページを開くと同時にメディアが再生されるサイトでは、出題対象と判定した時点でタブ全体をミュートする。

- 通常のページ遷移では、`webNavigation.onBeforeNavigate` で遷移開始を検知し、可能な限りページ読み込み前に対象タブをミュートする。
- Content Scriptは `document_start` で注入し、ページの初期描画段階からロックモーダルを構築する。
- SPA内の遷移でも、出題対象と判定した直後に対象タブをミュートする。
- モーダルは映像を含むページ全体を不透明に覆う。
- 動画自体が背後で再生を開始することは許容するが、正解するまでそのタブの音声を聞こえない状態にする。
- ミュート前のタブの状態を保存し、正解後は拡張機能がミュートした場合に限って元の状態へ戻す。
- 出題前から利用者がタブをミュートしていた場合は、正解後もミュートを維持する。
- 出題中にタブがミュート解除されたことを `tabs.onUpdated` で検知した場合は、正解前のため再度ミュートする。
- 出題中のリロードや、出題元のサイト設定に引き続き一致するURLへの遷移では、正解するまでミュート状態を維持する。
- 出題元のサイト設定に一致しないURLへ遷移した場合は、拡張機能がミュートした場合に限って元の状態へ戻す。

サイト側JavaScriptの実行や動画データの読み込みそのものは停止しない。Chrome拡張機能とサイト側処理の実行順序には差があるため、全サイトで「動画を1フレームも再生させない」ことまでは保証せず、利用者に映像を見せず、音声を聞かせないことを保証対象とする。

### 5.8 状態保持

- サイト設定は `chrome.storage.sync` に保存し、同じChromeアカウント間で同期可能にする。
- 共通表示設定も `chrome.storage.sync` に保存し、同じChromeアカウント間で同期可能にする。
- 出題判定用の日時・移動回数・タブごとの重複検知情報は `chrome.storage.local` に保存する。
- Service Workerが停止・再起動しても状態を復元できるよう、判定に必要な情報をメモリだけに置かない。
- 設定編集時は以下のように状態を扱う。
  - 間隔方式を変更した場合: 基準日時と移動回数をリセットする。
  - 間隔値を変更した場合: 既存の経過時間または移動回数を維持し、新しい閾値で次回判定する。
  - URL正規表現を変更した場合: 基準日時と移動回数をリセットする。
  - 無効化した場合: 状態は維持するが判定しない。
  - 削除した場合: 対応するローカル状態も削除する。
- 移動回数方式の対象URL遷移を処理した場合: `lastNavigationAt` を遷移日時へ更新する。
- 既存バージョンのローカル状態に `lastNavigationAt` が存在しない場合: 既存の移動回数を維持し、移行処理を行った日時を `lastNavigationAt` として12時間の猶予を与える。

## 6. 画面設計

### 6.1 設定画面

Chrome拡張機能のオプションページとして提供する。

#### 設定一覧領域

- サイト名
- URL正規表現
- `N分ごと` または `N回移動ごと` の要約
- 有効・無効スイッチ
- 編集ボタン
- 削除ボタン
- 新規追加ボタン

#### 編集フォーム

1. サイト名入力（任意）
2. URL正規表現入力
3. 間隔方式のラジオボタンまたはセレクトボックス
4. 1以上の整数入力
5. 保存ボタン
6. キャンセルボタン

URL正規表現欄には、現在開いているページを対象にするための入力例を表示する。

#### 共通表示設定領域

- 文字色のカラーピッカー
- 文字色のカラーコード入力欄
- 背景色のカラーピッカー
- 背景色のカラーコード入力欄
- 配色プレビュー
- 保存ボタン
- 初期値に戻すボタン

初期値に戻す操作では、文字色を `#c9d1d9`、背景色を `#000000` に戻す。

### 6.2 ロックモーダル

表示例:

```text
┌──────────────────────────────┐
│                              │
│          休憩タイム           │
│                              │
│          7 + 5 = ?           │
│                              │
│       [ 回答を入力       ]     │
│         [ 回答する ]          │
│                              │
│  ※正解するとページを表示します  │
└──────────────────────────────┘
```

## 7. システム構成

```text
┌────────────────────┐
│ Options Page       │
│ 設定の登録・編集     │
└─────────┬──────────┘
          │ chrome.storage.sync
          ▼
┌────────────────────┐       URL遷移通知       ┌────────────────────┐
│ Service Worker     │◀───────────────────────│ Content Script     │
│ ・対象URL判定       │                        │ ・SPA変更検知       │
│ ・間隔/回数判定     │──── 出題/解除指示 ────▶│ ・モーダル表示       │
│ ・状態の永続化      │                        │ ・回答受付           │
└─────────┬──────────┘                        └────────────────────┘
          │ chrome.storage.local
          ▼
┌────────────────────┐
│ Runtime State      │
│ 日時・回数・出題状態 │
└────────────────────┘
```

### 7.1 構成ファイル案

```text
browsing-breaks/
├─ manifest.json
├─ src/
│  ├─ background/
│  │  └─ service-worker.js
│  ├─ content/
│  │  ├─ content-script.js
│  │  └─ modal.css
│  ├─ options/
│  │  ├─ options.html
│  │  ├─ options.js
│  │  └─ options.css
│  └─ shared/
│     ├─ constants.js
│     ├─ storage.js
│     ├─ validation.js
│     └─ types.js
├─ icons/
└─ tests/
```

## 8. データ設計

### 8.1 同期設定

```json
{
  "settingsVersion": 1,
  "appearance": {
    "textColor": "#c9d1d9",
    "backgroundColor": "#000000"
  },
  "siteRules": [
    {
      "id": "uuid",
      "name": "Example",
      "urlPattern": "^https://(www\\.)?example\\.com/",
      "intervalType": "minutes",
      "intervalValue": 10,
      "enabled": true,
      "createdAt": 1788793200000,
      "updatedAt": 1788793200000
    }
  ]
}
```

`intervalType` は次のいずれかとする。

- `minutes`: 時間方式
- `navigations`: 移動回数方式

### 8.2 ローカル状態

```json
{
  "ruleStates": {
    "uuid": {
      "lastSolvedAt": 1788793200000,
      "navigationCount": 0,
      "lastNavigationAt": 1788793800000
    }
  },
  "activeChallenges": {
    "123": {
      "ruleId": "uuid",
      "url": "https://example.com/page2",
      "left": 7,
      "right": 5,
      "previouslyMuted": false,
      "mutedByExtension": true,
      "createdAt": 1788793800000
    }
  }
}
```

`activeChallenges` のキーはChromeのタブIDとする。問題と正解判定はService Worker側で管理し、ページ側から正解値を容易に読み取れないようにする。

## 9. 処理フロー

### 9.0 拡張機能インストール・更新時

1. Service Workerが `chrome.storage.sync` から `siteRules` キーを既定値なしで取得する。
2. `siteRules` キーが存在しない場合は、`createInitialSiteRules` に定義された初期サイト設定を生成して保存する。
3. `siteRules` キーが存在する場合は、空配列を含めて保存済みのサイト設定を維持する。
4. 表示設定には不足値の補完を適用し、現在の `settingsVersion` とともに保存する。

### 9.1 URL遷移時

1. Chrome Navigation APIがURL遷移を検知する。Content ScriptのURL監視は検知漏れを補完する。
2. Service WorkerへタブID、遷移元URL、遷移先URL、遷移種別を通知する。
3. Service Workerがタブ、到達URL、遷移種別を使って通知を論理URL遷移へ統合し、重複通知なら処理を終了する。
4. タブに出題中状態がある場合は、移動先URLが出題元ルールに一致するか確認する。一致する場合は同じ出題を維持して処理を終了する。一致しない場合は出題中状態を解除するが、ルール状態はリセットしない。
5. Service Workerが有効なサイト設定を優先順に照合する。
6. 方式に応じた基準日時から12時間以上経過している場合は状態を初期化し、現在の遷移では出題も加算も行わない。
7. 12時間未満の場合、一致した設定の時間または移動回数を更新・判定する。
8. 出題条件を満たさない場合は、Content Scriptへ解除通知を送り、解除した出題のミュート状態を元へ戻して処理を終了する。
9. 出題条件を満たした場合は、タブの元のミュート状態を記録してタブをミュートする。直前の出題から切り替わる場合は、その出題が保持していた元のミュート状態を引き継ぐ。
10. 問題を生成し、ミュート情報とともにローカル状態へ保存する。
11. Content Scriptへ問題を送り、ロックモーダルを表示する。

### 9.2 回答時

1. Content Scriptが入力値をService Workerへ送信する。
2. Service Workerが保存済み問題と照合する。
3. 不正解ならContent Scriptへ不正解を返す。
4. 正解なら該当設定の基準日時または移動回数をリセットする。
5. 拡張機能がミュートした状態が保たれている場合は、タブを出題前のミュート状態へ戻す。
6. 出題中状態を削除し、Content Scriptへ解除を指示する。

### 9.3 ページ再読み込み時

出題中にページが再読み込みされても解除されないよう、Content Script起動時にService Workerへ出題状態を問い合わせる。タブIDに対応する出題が残っており、方式別の基準日時から12時間未満であれば、タブのミュートを維持して同じ問題を再表示する。12時間以上経過していれば出題状態を削除し、状態を初期化してモーダルを表示しない。

## 10. Manifestと権限

想定する主な設定・権限:

- `manifest_version: 3`
- `storage`: 設定と実行状態の保存
- `tabs`: タブURLと遷移情報の取得
- `webNavigation`: 通常遷移およびSPA履歴更新の検知
- `host_permissions: ["http://*/*", "https://*/*"]`: 通常のWebサイト上で動作するため
- `content_scripts.matches: ["http://*/*", "https://*/*"]`
- `options_page` または `options_ui`: 設定画面
- `background.service_worker`: 判定と状態管理

権限が広いため、Chromeウェブストアへ公開する際は、全サイト権限が必要な理由を説明文とプライバシーポリシーに明記する。

## 11. セキュリティ・プライバシー

- 閲覧URLはサイト設定との照合にだけ使用し、外部サーバーへ送信しない。
- 足し算問題、回答、閲覧回数、時刻はローカル処理のみとする。
- 正規表現の評価では、極端に長い入力を制限し、破滅的バックトラッキングによる停止リスクを軽減する。
- Content Scriptから届くメッセージは型と送信元タブを検証する。
- ページDOMへ回答を属性やテキストとして保持しない。
- HTMLの組み立てに未検証のサイト名やURL文字列を使用せず、`textContent` を用いてXSSを防ぐ。

## 12. 非機能要件

- 通常閲覧時に体感できる遅延を発生させない。
- URL判定と状態更新は、1回の遷移につき原則100ms以内を目標とする。
- キーボードだけで回答できること。
- モーダルは主要なデスクトップ画面幅で崩れず、狭い画面でも利用できること。
- Service Workerの再起動、ブラウザ再起動、ページ再読み込み後も判定状態を維持すること。
- 保存形式に `settingsVersion` を持たせ、将来の設定移行を可能にする。

## 13. テスト方針

### 13.1 単体テスト

- 正常・異常なURL正規表現の検証
- URLと複数ルールの一致判定、優先順位
- 1以上の安全な整数の境界値検証
- `#RRGGBB` カラーコードの正常値・異常値検証
- 共通表示設定が存在しない保存データへの初期色の補完
- `createInitialSiteRules` が定義どおりの設定、一意なIDおよび作成・更新日時を生成すること
- 時間方式の期限前、期限到達、期限超過
- 移動回数方式の1回目、閾値直前、閾値到達、リセット後
- 足し算の正解・不正解判定
- 設定変更時の状態リセット規則
- 重複遷移イベントの抑止
- SPAの同一到達URLが `webNavigation`、Content Scriptの順で通知されても1回だけ加算されること
- SPAの同一到達URLがContent Script、`webNavigation`の順で通知されても1回だけ加算されること
- 同一到達URLの通知間隔が重複判定時間を超えても、SPAでは再加算されないこと
- 同じURLへのリロードはSPAの重複通知と区別され、1回加算されること
- 移動回数方式で最後の移動から12時間未満、ちょうど12時間、12時間超過の境界判定
- 時間方式で最後の基準日時から12時間未満、ちょうど12時間、12時間超過の境界判定
- 12時間経過を検出した遷移では出題せず、移動回数にも加算しないこと
- 12時間経過後の次の対象URL遷移から通常判定を再開すること
- 旧形式の状態へ `lastNavigationAt` を補完しても既存カウントが失われないこと

### 13.2 結合テスト

- 通常ページ間の遷移で期待回数に出題される。
- SPA内のURL変更で期待回数に出題される。
- 時間経過中は出題せず、経過後の次回遷移で出題される。
- 不正解、空欄、数字以外ではモーダルが解除されない。
- 正解時だけモーダルが解除され、カウンターがリセットされる。
- 自動再生サイトでは、正解するまでタブの音声が聞こえない。
- 正解後は、利用者が出題前に設定していたミュート状態へ正しく戻る。
- 出題中のリロード後も同じ問題が再表示される。
- 複数タブでも状態が壊れない。
- 無効化した設定では出題されない。
- `siteRules` キーが存在しない場合だけ初期サイト設定が作成され、空配列または既存ルールが保存されている場合は追加されない。
- 設定した文字色と背景色が、すべての対象サイトのモーダルへ共通して反映される。
- 出題中に配色を変更した場合、表示中のモーダルへ即時反映される。
- 初期値に戻すと、文字色が `#c9d1d9`、背景色が `#000000` になる。
- ブラウザ終了、スリープ、またはページを開いたままの未使用状態を問わず、方式別の基準日時から12時間以上経過した後の最初の対象URL遷移では出題されず、状態が初期化される。
- 12時間以上経過した古い出題状態はページ復元時に再表示されず、拡張機能が変更したミュート状態だけが元に戻る。
- 出題中に出題元ルールと一致しない通常のWebサイトへ移動すると、移動先ではモーダルが表示されず、ページを閲覧・操作できる。
- 出題中に出題元ルールと一致する別URLへ移動すると、同じ問題が維持される。
- 出題元ルールと一致しないサイトへ移動しても、出題元ルールの基準日時または移動回数はリセットされず、対象サイトへ戻ることで出題を恒久的に回避できない。
- 出題中に別の出題対象サイトへ移動した場合、旧出題のミュート状態が正しく新しい出題へ引き継がれる。
- 出題元ルールに一致しないサイトへ移動した際、Service Workerの出題情報だけでなく、同一ドキュメント上に表示済みのモーダルも解除される。

## 14. 受け入れ条件

1. 利用者はURL正規表現、間隔方式、1以上の整数の間隔値を保存でき、任意でサイト名を付けられる。
2. 利用者は全サイト共通の文字色と背景色を設定できる。
3. 初期状態では文字色が `#c9d1d9`、背景色が `#000000` になる。
4. `siteRules` が未作成の場合は `createInitialSiteRules` で定義された初期設定が作成され、既存の `siteRules` は上書きされない。
5. URL正規表現に一致しないページではモーダルが表示されない。
6. 時間方式では、指定分数経過後の次の対象URL遷移でモーダルが表示される。
7. 移動回数方式では、指定回数目の対象URL遷移でモーダルが表示される。
8. モーダル表示中は背景ページの内容を閲覧・操作できない。
9. モーダル表示中は、対象タブで自動再生された動画・音声の音が聞こえない。
10. 不正解では解除されず、正解の場合のみ解除される。
11. 正解後は、拡張機能が変更したタブのミュート状態だけが元に戻る。
12. リロードやService Worker再起動で、出題間隔と出題中状態が失われない。
13. 通常遷移とSPA内遷移の両方を検知できる。
14. 時間方式の基準日時または移動回数方式の最終移動日時から12時間以上経過した場合、次の対象URL遷移では出題せず状態を初期化できる。
15. 出題中でも出題元ルールに一致しないサイトへ移動でき、そのサイトではロックモーダルと拡張機能によるミュートが解除される。

## 15. 初期仕様で採用する解釈

元の要望で実装方法が複数考えられる点は、初期版では次のように定義する。

- 「何分ごと」は、時間が経過した直後ではなく、その後の次回対象URL遷移で出題する。
- 「移動ごと」は、対象URLへの遷移回数を数える。
- 1以上の整数の間隔値は、選択方式に応じて分または回数として使用する。
- 足し算に使う2つの数字は、それぞれ1～9とする。
- カウンターと時刻はタブ単位ではなくサイト設定単位で共有する。
- リロードは1回の移動として数え、単なる初回表示は数えない。
- 同じURLに複数設定が一致した場合は、設定一覧の優先順で1問だけ表示する。
- SPA遷移はサイト固有のDOMやURL形式ではなく、Chrome Navigation APIと到達URLに基づいて汎用的に重複排除する。
- 12時間経過時の初期化はルール単位で行い、時間方式は `lastSolvedAt`、移動回数方式は `lastNavigationAt` を基準にする。
- 12時間経過を検出した現在の遷移はカウントせず、次の対象URL遷移から再開する。
- 出題の拘束範囲はタブ全体ではなく出題元のサイト設定に一致するURLとし、一致しないサイトへの移動は妨げない。

これらは設定項目の追加や仕様変更に応じて変更可能とする。

## 16. 将来拡張候補

- 設定画面、ロックモーダル、エラーメッセージの多言語対応（Chrome拡張機能の `_locales` / `chrome.i18n` を利用）
- 足し算の数字範囲や難易度設定
- 引き算、掛け算、割り算
- 曜日・時間帯による有効化
- 一時停止機能
- 設定のインポート・エクスポート
- 正解数や中断回数の統計
- URL正規表現のテスト機能
- 設定一覧のドラッグ操作による優先順位変更
